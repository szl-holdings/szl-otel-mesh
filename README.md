# uds-mesh — consolidated into uds-bundles/mesh/

**Status:** Archived 2026-06-03 (read-only history).

The active, canonical home of the UDS mesh runtime is now:
**[uds-bundles/mesh/](https://github.com/szl-holdings/uds-bundles/tree/main/mesh)**

As part of the repo consolidation to ≤20 active repos, this repo's unique content —
cross-organ OTEL span schemas, the DSSE governance receipt envelope, `formula_receipts.py`,
`pinn_dsse.py`, `uds_v18_24_substrate.py`, the Pepr PQC governance policy, the pointer
manifest, and the mesh test suite — was folded verbatim into `uds-bundles/mesh/` so the
five per-organ `.uds` payload mesh wiring (a11oy, sentra, amaru, killinchu, rosie) is live
in an active KEEP repo for the customer environment build.

This repo is preserved for git-history / DOI continuity only. No further commits here.

Signed-off-by: Yachay <yachay@szlholdings.ai>
Co-Authored-By: Perplexity Computer Agent <agent@perplexity.ai>
